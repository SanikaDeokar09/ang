Here are the **neat practical steps** for your Angular User Authentication project. 

---

# Angular User Authentication Practical

## Aim

Create Angular application for:

* User Registration
* User Login
* User Profile Display

---

# STEP 1 — Install Node.js

Go to:

[Node.js Official Website](https://nodejs.org?utm_source=chatgpt.com)

Install:

* LTS Version

After installation check:

```bash
node -v
npm -v
```

---

# STEP 2 — Install Angular CLI

Open terminal:

```bash
npm install -g @angular/cli
```

Check installation:

```bash
ng version
```

---

# STEP 3 — Create Angular Project

```bash
ng new ang-app
```

Questions asked:

```text
Would you like to add Angular routing? → Yes
Which stylesheet format? → CSS
```

---

# STEP 4 — Open Project

```bash
cd ang-app
code .
```

---

# STEP 5 — Run Angular Project

```bash
ng serve
```

Open:

```text
http://localhost:4200
```

Angular default page appears.

---

# STEP 6 — Generate Components

```bash
ng g c register
ng g c login
ng g c profile
```

Creates:

* Register Component
* Login Component
* Profile Component

---

# STEP 7 — Configure Routes

Open:

```text
src/app/app.routes.ts
```

Replace code:

```ts
import { Routes } from '@angular/router';

import { Register } from './register/register';
import { Login } from './login/login';
import { Profile } from './profile/profile';

export const routes: Routes = [

  { path: '', component: Register },

  { path: 'login', component: Login },

  { path: 'profile', component: Profile }

];
```

---

# STEP 8 — Configure App

Open:

```text
src/app/app.config.ts
```

Replace:

```ts
import { ApplicationConfig } from '@angular/core';
import { provideRouter } from '@angular/router';

import { routes } from './app.routes';

export const appConfig: ApplicationConfig = {
  providers: [
    provideRouter(routes)
  ]
};
```

---

# STEP 9 — Configure Main App

Open:

```text
src/app/app.ts
```

Replace:

```ts
import { Component } from '@angular/core';
import { RouterOutlet } from '@angular/router';

@Component({
  selector: 'app-root',
  standalone: true,
  imports: [RouterOutlet],
  templateUrl: './app.html',
  styleUrl: './app.css'
})
export class App {

}
```

---

# STEP 10 — App HTML

Open:

```text
src/app/app.html
```

Replace:

```html
<router-outlet></router-outlet>
```

---

# STEP 11 — Register Component

## register.ts

Open:

```text
src/app/register/register.ts
```

Paste:

```ts
import { Component } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { Router, RouterLink } from '@angular/router';

@Component({
  selector: 'app-register',
  standalone: true,
  imports: [FormsModule, RouterLink],
  templateUrl: './register.html',
  styleUrl: './register.css'
})
export class Register {

  user = {
    name: '',
    email: '',
    password: ''
  };

  constructor(private router: Router) {}

  registerUser() {

    localStorage.setItem('user', JSON.stringify(this.user));

    alert('Registration Successful');

    this.router.navigate(['/login']);
  }
}
```

---

## register.html

```html
<div class="container">

    <h2>Register User</h2>

    <form (ngSubmit)="registerUser()">

        <input
        type="text"
        placeholder="Enter Name"
        [(ngModel)]="user.name"
        name="name"
        required>

        <input
        type="email"
        placeholder="Enter Email"
        [(ngModel)]="user.email"
        name="email"
        required>

        <input
        type="password"
        placeholder="Enter Password"
        [(ngModel)]="user.password"
        name="password"
        required>

        <button type="submit">Register</button>

    </form>

    <p>
        Already have account?
        <a routerLink="/login">Login</a>
    </p>

</div>
```

---

## register.css

```css
.container{
    width: 450px;
    margin: 80px auto;
    padding: 45px;
    border-radius: 20px;
    background: white;
    box-shadow: 0 0 25px rgba(0,0,0,0.2);
    text-align: center;
}

h2{
    margin-bottom: 25px;
    color: #333;
}

input{
    width: 100%;
    padding: 15px;
    margin: 15px 0;
    border: 1px solid #ccc;
    border-radius: 10px;
    font-size: 16px;
    box-sizing: border-box;
}

button{
    width: 100%;
    padding: 15px;
    border: none;
    border-radius: 10px;
    background: #4a90e2;
    color: white;
    font-size: 18px;
    cursor: pointer;
}
```

---

# STEP 12 — Login Component

## login.ts

```ts
import { Component } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { Router } from '@angular/router';

@Component({
  selector: 'app-login',
  standalone: true,
  imports: [FormsModule],
  templateUrl: './login.html',
  styleUrl: './login.css'
})
export class Login {

  email = '';
  password = '';

  constructor(private router: Router) {}

  loginUser() {

    const data:any = localStorage.getItem('user');

    const user = JSON.parse(data);

    if (
      this.email == user.email &&
      this.password == user.password
    ) {

      alert('Login Successful');

      this.router.navigate(['/profile']);

    } else {

      alert('Invalid Email or Password');

    }
  }
}
```

---

## login.html

```html
<div class="container">

    <h2>Login User</h2>

    <form (ngSubmit)="loginUser()">

        <input
        type="email"
        placeholder="Enter Email"
        [(ngModel)]="email"
        name="email"
        required>

        <input
        type="password"
        placeholder="Enter Password"
        [(ngModel)]="password"
        name="password"
        required>

        <button type="submit">Login</button>

    </form>

</div>
```

---

## login.css

```css
.container{
    width: 450px;
    margin: 80px auto;
    padding: 45px;
    border-radius: 20px;
    background: white;
    box-shadow: 0 0 25px rgba(0,0,0,0.2);
    text-align: center;
}
```

---

# STEP 13 — Profile Component

## profile.ts

```ts
import { Component, OnInit } from '@angular/core';

@Component({
  selector: 'app-profile',
  standalone: true,
  imports: [],
  templateUrl: './profile.html',
  styleUrl: './profile.css'
})
export class Profile implements OnInit {

  user:any;

  ngOnInit(): void {

    const data:any = localStorage.getItem('user');

    this.user = JSON.parse(data);
  }
}
```

---

## profile.html

```html
<div class="container">

    <h2>User Profile</h2>

    <p><b>Name:</b> {{user.name}}</p>

    <p><b>Email:</b> {{user.email}}</p>

</div>
```

---

## profile.css

```css
.container{
    width: 500px;
    margin: 100px auto;
    padding: 50px;
    border-radius: 20px;
    background: white;
    box-shadow: 0 0 25px rgba(0,0,0,0.2);
    text-align: center;
}
```

---

# STEP 14 — Global Styling

Open:

```text
src/styles.css
```

Paste:

```css
body{
    margin: 0;
    padding: 0;
    background: linear-gradient(to right, #4a90e2, #6dd5fa);
    min-height: 100vh;
    font-family: Arial, sans-serif;
}
```

---

# STEP 15 — Remove Angular Logo

Delete:

```text
public/favicon.ico
```

Open:

```text
src/index.html
```

Remove:

```html
<link rel="icon" type="image/x-icon" href="favicon.ico">
```

Change title:

```html
<title>User Authentication System</title>
```

---

# STEP 16 — Run Project

```bash
ng serve
```

Open:

```text
http://localhost:4200
```

---

# Output

1. Registration Page
2. Login Page
3. Profile Page Showing User Data
